# L23 表格重建稽核報告

- 候選表格：5
- 信心分布：{'high': 5}
- 低信心（需人工覆核）：0

## p21　2 欄 × 7 列　`high`

原文：
```
   主題                必記結論
   損失函數            高斯 →MSE（敏感）；拉普拉斯 →MAE（穩健）；分類 →交叉熵
   貝氏                低盛行率下陽性預測值可能極低（基率謬誤）
   單純貝氏            條件獨立假設多不成立但仍實用；機率校準不佳
   偏差-變異            兩線都高且收斂＝高偏差（加資料無用）；差距大＝高變異（加資料有
                   用）
   不可約誤差          無法消除，決定效能上限
   ddof                                                             numpy預設  0；pandas預設  1
```

重建：

| 主題 | 必記結論 |
|---|---|
| 損失函數 | 高斯 →MSE（敏感）；拉普拉斯 →MAE（穩健）；分類 →交叉熵 |
| 貝氏 | 低盛行率下陽性預測值可能極低（基率謬誤） |
| 單純貝氏 | 條件獨立假設多不成立但仍實用；機率校準不佳 |
| 偏差-變異 | 兩線都高且收斂＝高偏差（加資料無用）；差距大＝高變異（加資料有用） |
| 不可約誤差 | 無法消除，決定效能上限 |
| ddof | numpy預設 0；pandas預設 1 |

## p50　2 欄 × 8 列　`high`

原文：
```
    主題                必記結論
    集成                                                            Bagging降變異（深樹）／  Boosting降偏差（淺樹）
    隨機森林                                              Bagging＋特徵抽樣；加樹幾乎不過擬合
    Boosting             樹數是關鍵超參數，須早停
    學習率              調小則需更多樹
    縮放                只有樹模型家族不需要
    predict_proba        回傳  (n,類別數)；二元須取[:, 1]
    DBSCAN            任意形狀＋自動標雜訊（−1）
```

重建：

| 主題 | 必記結論 |
|---|---|
| 集成 | Bagging降變異（深樹）／ Boosting降偏差（淺樹） |
| 隨機森林 | Bagging＋特徵抽樣；加樹幾乎不過擬合 |
| Boosting | 樹數是關鍵超參數，須早停 |
| 學習率 | 調小則需更多樹 |
| 縮放 | 只有樹模型家族不需要 |
| predict_proba | 回傳 (n,類別數)；二元須取[:, 1] |
| DBSCAN | 任意形狀＋自動標雜訊（−1） |

## p70　3 欄 × 7 列　`high`

原文：
```
    任務               輸出神經元數       激活函數           損失函數
    二元分類                                          1                                                                   sigmoid             二元交叉熵
    多元單標籤分類         C                                                                 softmax           類別交叉熵
    多元多標籤分類         C                                                                 sigmoid            二元交叉熵
    迴歸                                                        1                  線性                                                        MSE／  MAE／  Huber
  常見陷阱與錯誤觀念
  softmax與多標籤不相容——softmax強制各類機率總和為  1，互斥假設會使多標籤任務無法正確建
  模。此為高頻陷阱。
```

重建：

| 任務 | 輸出神經元數 激活函數 損失函數 |  |
|---|---|---|
| 二元分類 | 1 | sigmoid 二元交叉熵 |
| 多元單標籤分類 | C | softmax 類別交叉熵 |
| 多元多標籤分類 | C | sigmoid 二元交叉熵 |
| 迴歸 | 1 線性 | MSE／ MAE／ Huber |
| 常見陷阱與錯誤觀念softmax與多標籤不相容——softmax強制各類機率總和為 | 1，互斥假設會使多標籤任務無法正確建 |  |
| 模。此為高頻陷阱。 |  |  |

## p73　2 欄 × 6 列　`high`

原文：
```
   主題                必記結論
   五步流程            任務型態 →資料 →限制 →基準線 →逐步加複雜度
   基準線              分類猜多數類／迴歸猜平均／時序等於前一期
   輸出層              二元  1+sigmoid；多元單標籤 C+softmax；多標籤 C+sigmoid
   softmax陷阱         單一神經元時輸出恆為  1；多標籤不可用
   限制導向            要解釋 →線性或樹；要低延遲 →小模型；表格 →梯度提升樹
```

重建：

| 主題 | 必記結論 |
|---|---|
| 五步流程 | 任務型態 →資料 →限制 →基準線 →逐步加複雜度 |
| 基準線 | 分類猜多數類／迴歸猜平均／時序等於前一期 |
| 輸出層 | 二元 1+sigmoid；多元單標籤 C+softmax；多標籤 C+sigmoid |
| softmax陷阱 | 單一神經元時輸出恆為 1；多標籤不可用 |
| 限制導向 | 要解釋 →線性或樹；要低延遲 →小模型；表格 →梯度提升樹 |

## p101　2 欄 × 7 列　`high`

原文：
```
   主題                必記結論
   六種攻擊            毒化（訓練期）／對抗樣本／模型竊取／成員推論／訓練資料抽取／
                  模型反演
   生成式風險          提示注入（直接與間接）、越獄、不安全輸出處理、過度代理、資訊揭
                  露、供應鏈
   護欄五層            輸入／檢索／模型／輸出／行動
   鐵律一              權限在系統層，不在提示層
   鐵律二              模型輸出視同不可信輸入
   稽核                高風險系統須留存模型版本、輸入摘要、輸出、信心值、覆核紀錄
```

重建：

| 主題 | 必記結論 |
|---|---|
| 六種攻擊 | 毒化（訓練期）／對抗樣本／模型竊取／成員推論／訓練資料抽取／模型反演 |
| 生成式風險 | 提示注入（直接與間接）、越獄、不安全輸出處理、過度代理、資訊揭露、供應鏈 |
| 護欄五層 | 輸入／檢索／模型／輸出／行動 |
| 鐵律一 | 權限在系統層，不在提示層 |
| 鐵律二 | 模型輸出視同不可信輸入 |
| 稽核 | 高風險系統須留存模型版本、輸入摘要、輸出、信心值、覆核紀錄 |
