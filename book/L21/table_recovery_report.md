# L21 表格重建稽核報告

- 候選表格：7
- 信心分布：{'high': 7}
- 低信心（需人工覆核）：0

## p25　2 欄 × 9 列　`high`

原文：
```
     主題                必記結論
     文本表示                                              One-Hot無語意；TF-IDF無語意但有權重；Word2Vec靜態一詞一向
                       量；BERT類為上下文嵌入
     TF-IDF             出現在所有文件的詞，IDF = log1 = 0，權重歸零
     RNN                梯度消失源於梯度連乘
     LSTM／  GRU                          LSTM三門＋細胞狀態；GRU兩門、無獨立細胞狀態
     自注意力            除以 √dk防飽和；位置編碼補詞序；複雜度 O(n2d)，優勢在並行非
                       省算力
     架構選型            理解  BERT／生成  GPT／轉換  T5
     指標                                                            BLEU精確率（翻譯）；ROUGE召回率（摘要）；Perplexity越低越
                       好；NER用  entity-level  F1
```

重建：

| 主題 | 必記結論 |
|---|---|
| 文本表示 | One-Hot無語意；TF-IDF無語意但有權重；Word2Vec靜態一詞一向量；BERT類為上下文嵌入 |
| TF-IDF | 出現在所有文件的詞，IDF = log1 = 0，權重歸零 |
| RNN | 梯度消失源於梯度連乘 |
| LSTM／ GRU | LSTM三門＋細胞狀態；GRU兩門、無獨立細胞狀態 |
| 自注意力 | 除以 √dk防飽和；位置編碼補詞序；複雜度 O(n2d)，優勢在並行非省算力 |
| 架構選型 | 理解 BERT／生成 GPT／轉換 T5 |
| 指標 | BLEU精確率（翻譯）；ROUGE召回率（摘要）；Perplexity越低越 |
|  | 好；NER用 entity-level F1 |

## p30　3 欄 × 5 列　`high`

原文：
```
   情境                     閾值方向                 代價
   漏檢成本高（安全件、醫        調低閾值→提高召回率   誤報增加，複檢人力上升
   材、航太）
   誤報成本高（自動報廢、自       調高閾值→提高精確率   漏檢增加
   動封鎖）
```

重建：

| 情境 | 閾值方向 | 代價 |
|---|---|---|
| 漏檢成本高（安全件、醫 | 調低閾值→提高召回率 | 誤報增加，複檢人力上升 |
| 材、航太） |  |  |
| 誤報成本高（自動報廢、自 | 調高閾值→提高精確率 | 漏檢增加 |
| 動封鎖） |  |  |

## p59　4 欄 × 7 列　`high`

原文：
```
 選項          適用                     優勢                     風險
 自建          核心競爭力、資料極敏        完全掌控、資料不出境、        人才門檻、時程長、維運責
            感、需求高度客製           長期邊際成本低            任全在己方
 採購          通用能力、需求標準化、       上市快、免維運           供應商鎖定、資料可能傳
            需快速上線                                 輸境外、成本隨用量線性
                                                  成長、版本被動更新
 混合          多數企業的現實解         通用任務外購，敏感任務         架構複雜度與治理難度提
                               地端                 高
```

重建：

| 選項 | 適用 | 優勢 | 風險 |
|---|---|---|---|
| 自建 | 核心競爭力、資料極敏 | 完全掌控、資料不出境、 | 人才門檻、時程長、維運責 |
|  | 感、需求高度客製 | 長期邊際成本低 | 任全在己方 |
| 採購 | 通用能力、需求標準化、 | 上市快、免維運 | 供應商鎖定、資料可能傳 |
|  | 需快速上線 |  | 輸境外、成本隨用量線性成長、版本被動更新 |
| 混合 | 多數企業的現實解 | 通用任務外購，敏感任務 | 架構複雜度與治理難度提 |
|  |  | 地端 | 高 |

## p63　2 欄 × 9 列　`high`

原文：
```
   主題                必記結論
   四層可行性          業務→資料→技術→營運；最常死在資料層
   效能基準            現行流程／人類／簡單方法，三個都要
   TCO                建置、營運、維護、組織；標註與維運最常被低估
   ROI                 分子是淨效益；注意成本的發生頻率
   NPV                多年期比較必用；ROI忽略時間價值
   效益型態            成本節省／營收增長可量化；無形效益分開陳列
   階段閘門                                              POC（做得到）／  POV（划算）／  Pilot（行得通）／  Production（穩
                    得住）
   高風險              預判前移，合規成本計入  TCO
```

重建：

| 主題 | 必記結論 |
|---|---|
| 四層可行性 | 業務→資料→技術→營運；最常死在資料層 |
| 效能基準 | 現行流程／人類／簡單方法，三個都要 |
| TCO | 建置、營運、維護、組織；標註與維運最常被低估 |
| ROI | 分子是淨效益；注意成本的發生頻率 |
| NPV | 多年期比較必用；ROI忽略時間價值 |
| 效益型態 | 成本節省／營收增長可量化；無形效益分開陳列 |
| 階段閘門 | POC（做得到）／ POV（划算）／ Pilot（行得通）／ Production（穩得住） |
| 高風險 | 預判前移，合規成本計入 TCO |

## p86　2 欄 × 8 列　`high`

原文：
```
  缺失率           建議處置                     注意
  低（約  5%以下） 刪除該列或簡單插補（平均／         須確認缺失是否隨機發生
              中位／眾數）
  中               模型插補（如以其他欄位預測），  缺失本身常帶有資訊——沒填手機號碼可能
              並新增缺失指示欄             就代表低黏著度客戶
  高（過半）       多半應刪除該欄位，或僅保留        強行插補會製造大量假資料
              「是否有值」的二元特徵
 常見陷阱與錯誤觀念
 以  0填補數值型缺值是危險的，因為  0通常是一個合法值。模型會把「沒填」誤解為「數值為零」。
 正確作法是使用中位數等統計量搭配缺失指示欄。
```

重建：

| 缺失率 建議處置 | 注意 |
|---|---|
| 低（約 5%以下） 刪除該列或簡單插補（平均／ | 須確認缺失是否隨機發生 |
| 中位／眾數）中模型插補（如以其他欄位預測）， | 缺失本身常帶有資訊——沒填手機號碼可能 |
| 並新增缺失指示欄 | 就代表低黏著度客戶 |
| 高（過半） 多半應刪除該欄位，或僅保留 | 強行插補會製造大量假資料 |
| 「是否有值」的二元特徵 |  |
| 常見陷阱與錯誤觀念以0填補數值型缺值是危險的，因為0通常是一個合法值。模型會把「沒填」誤解為「數值為零」。 |  |
| 正確作法是使用中位數等統計量搭配缺失指示欄。 |  |

## p92　2 欄 × 8 列　`high`

原文：
```
   主題                必記結論
   順序                先切分，後轉換；重採樣只對訓練集
   缺失值              低刪／中插補＋指示欄／高刪欄；勿以  0填數值型
   離群值              上下界 =Q3+1.5IQR、Q1−1.5IQR；異常偵測中不可刪
   編碼                無序  One-Hot、有序順序編碼、高基數目標編碼（須折外）
   縮放                只有樹模型家族不需要
   選型                表格→梯度提升樹；小樣本→遷移學習；要解釋→線性或樹
   洩漏六源            前處理／重採樣／目標編碼／時序／分組／測試集調參
```

重建：

| 主題 | 必記結論 |
|---|---|
| 順序 | 先切分，後轉換；重採樣只對訓練集 |
| 缺失值 | 低刪／中插補＋指示欄／高刪欄；勿以 0填數值型 |
| 離群值 | 上下界 =Q3+1.5IQR、Q1−1.5IQR；異常偵測中不可刪 |
| 編碼 | 無序 One-Hot、有序順序編碼、高基數目標編碼（須折外） |
| 縮放 | 只有樹模型家族不需要 |
| 選型 | 表格→梯度提升樹；小樣本→遷移學習；要解釋→線性或樹 |
| 洩漏六源 | 前處理／重採樣／目標編碼／時序／分組／測試集調參 |

## p102　2 欄 × 10 列　`high`

原文：
```
   主題                必記結論
   推論型態            批次／線上／串流／邊緣；延遲看  P95、P99而非平均
   發布策略            影子（真實流量不影響客戶）／金絲雀（逐步放量）／藍綠（快速切換）
                  ／  A/B（比業務指標）
   MLOps              程式碼＋資料＋模型三者都變→需持續訓練
   偏斜                訓練與服務特徵邏輯不一致；根治靠共用同一份計算邏輯
   兩種漂移            資料漂移不需標籤即可偵測；概念漂移必須有標籤
   PSI                             <0.1穩定／  0.1–0.25注意／ >0.25須介入
   重訓                偵測→歸因→決策；勿跳過歸因直接重訓
   治理                高風險系統：日誌、監控、審查核准、獨立稽核；模型更新＝系統變
                  更
```

重建：

| 主題 | 必記結論 |
|---|---|
| 推論型態 | 批次／線上／串流／邊緣；延遲看 P95、P99而非平均 |
| 發布策略 | 影子（真實流量不影響客戶）／金絲雀（逐步放量）／藍綠（快速切換） |
|  | ／ A/B（比業務指標） |
| MLOps | 程式碼＋資料＋模型三者都變→需持續訓練 |
| 偏斜 | 訓練與服務特徵邏輯不一致；根治靠共用同一份計算邏輯 |
| 兩種漂移 | 資料漂移不需標籤即可偵測；概念漂移必須有標籤 |
| PSI | <0.1穩定／ 0.1–0.25注意／ >0.25須介入 |
| 重訓 | 偵測→歸因→決策；勿跳過歸因直接重訓 |
| 治理 | 高風險系統：日誌、監控、審查核准、獨立稽核；模型更新＝系統變更 |
